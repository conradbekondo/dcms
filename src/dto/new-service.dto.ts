export interface INewServiceDto {
    id?: string;
    name: string;
    description?: string;
    standardPrice: string;
    processingDuration?: string;
    operation?: 'update';
} // npm i -g yarn
// npm i -g @nestjs/cli
// yarn install
//  